---
name: Flex Display
property: display
parent: Flexbox
route: /flexbox/flex-display
previousRoute: /flexbox/flex-direction
previousLabel: Flex Direction
nextRoute: /flexbox/flex-grow
nextLabel: Flex Grow
---

import { Box } from "benefit-react"
import InfoBox from "../../components/InfoBox"
import Grid from "../../components/Grid"

import UtilityInfo from "../../components/UtilityInfo"

<UtilityInfo pattern="flex|inline-flex" />

---

<Grid>
  <InfoBox pattern="flex">
    <Box className="flex bg-blue-100 p-1 rounded">
      {[1, 2, 3].map((item) => (
        <Box
          key={`display-flex-${item}`}
          className="text-sm bold flex items-center justify-center bg-blue-400 rounded-sm text-blue-100 h-8 m-1 flex-grow"
        >
          {item}
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox pattern="inline-flex">
    <Box className="flex bg-blue-100 p-1 rounded">
      {[1, 2, 3].map((item) => (
        <Box
          key={`display-inline-flex-${item}`}
          className="text-sm bold flex items-center justify-center bg-blue-400 rounded-sm text-blue-100 h-8 w-8 m-1"
        >
          {item}
        </Box>
      ))}
    </Box>
  </InfoBox>
</Grid>
