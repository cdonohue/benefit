---
name: Background Color
property: background-color
parent: Background
route: /background/background-color
previousRoute: /background/background-attachment
previousLabel: Background Attachment
nextRoute: /background/background-position
nextLabel: Background Position
---

import { Playground } from "docz"
import InfoBox from "../../components/InfoBox"
import Grid from "../../components/Grid"
import { Box } from "benefit-react"
import UtilityInfo from "../../components/UtilityInfo"

<UtilityInfo pattern="bg-{color}" themeProperty="backgroundColor" hasColors />

---

_Shown here using the `blue` palette_

<Grid>
  {[...Array(9).keys()].reverse().map((level) => (
    <InfoBox pattern={`bg-blue-${(level + 1) * 100}`}>
      <Box
        key={level}
        className={`rounded-sm h-16 bg-blue-${(level + 1) * 100}`}
      />
    </InfoBox>
  ))}
</Grid>
