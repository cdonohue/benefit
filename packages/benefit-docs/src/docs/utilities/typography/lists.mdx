---
name: Lists
property: list-style-type
parent: Typography
route: /typography/lists/
previousRoute: /typography/line-height/
previousLabel: Line Height
nextRoute: /typography/style-decoration/
nextLabel: Style and Decoration
---

<UtilityInfo
  label="List Style Type"
  pattern="list-{type}"
  themeProperty="listStyleType"
/>

---

<ConfigConsumer>
  {({ config }) => (
    <Grid minWidth="200px">
      {Object.keys(config.theme.listStyleType).map(type => (
        <InfoBox key={`list-${type}`} pattern={`list-${type}`}>
          <Box className={`${type !== "none" ? "pl-4" : ""} text-sm`}>
            <Box is="ul" className={`list-${type}`}>
              <Box is="li" className="bg-gray-200 rounded-sm h-5 mb-2" />
              <Box is="li" className="bg-gray-200 rounded-sm h-5 w-1/2 mb-2" />
              <Box is="li" className="bg-gray-200 rounded-sm h-5 w-2/3" />
            </Box>
          </Box>
        </InfoBox>
      ))}
    </Grid>
  )}
</ConfigConsumer>

<UtilityInfo label="List Style Position" pattern="list-{inside|outside}" />

---

<Grid>
  <InfoBox pattern="list-inside">
    <Box className="pl-4">
      <Box is="ul" className="list-inside">
        <Box is="li" className="bg-gray-200 rounded-sm h-5 mb-2" />
        <Box is="li" className="bg-gray-200 rounded-sm h-5 w-1/2 mb-2" />
        <Box is="li" className="bg-gray-200 rounded-sm h-5 w-2/3" />
      </Box>
    </Box>
  </InfoBox>
  <InfoBox pattern="list-outside">
    <Box className="pl-4">
      <Box is="ul" className="list-outside">
        <Box is="li" className="bg-gray-200 rounded-sm h-5 mb-2" />
        <Box is="li" className="bg-gray-200 rounded-sm h-5 w-1/2 mb-2" />
        <Box is="li" className="bg-gray-200 rounded-sm h-5 w-2/3" />
      </Box>
    </Box>
  </InfoBox>
</Grid>
