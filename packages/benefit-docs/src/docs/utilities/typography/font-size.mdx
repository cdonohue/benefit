---
name: Font Size
property: font-size
parent: Typography
route: /typography/font-size
previousRoute: /typography/font-family
previousLabel: Font Family
nextRoute: /typography/font-weight
nextLabel: Font Weight
---

<UtilityInfo pattern="text-{size}" themeProperty="fontSize" />

---

<ConfigConsumer>
  {({ config }) => (
    <Grid>
      {Object.keys(config.theme.fontSize).map(size => (
        <InfoBox key={`text-${size}`} pattern={`text-${size}`}>
          <Box className={`bg-white rounded text-${size}`}>
            The five boxing wizards jump quickly.
          </Box>
        </InfoBox>
      ))}
    </Grid>
  )}
</ConfigConsumer>
