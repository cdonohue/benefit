---
name: Flex Direction
property: flex-direction
parent: Flexbox
route: /flexbox/flex-direction
previousRoute: /flexbox/flex
previousLabel: Flex
nextRoute: /flexbox/flex-display
nextLabel: Flex Display
---

<UtilityInfo pattern="flex-{row|row-reverse|col|col-reverse}" />

---

<Grid minWidth="175px">
  <InfoBox pattern="flex-row">
    <Box className="flex flex-row bg-gray-200 p-1 rounded">
      {[1, 2, 3].map(item => (
        <Box
          key={`flex-row-${item}`}
          className="text-sm bold flex items-center justify-center bg-gray-500 rounded-sm text-gray-100 h-8 w-8 m-1"
        >
          {item}
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox pattern="flex-row-reverse">
    <Box className="flex flex-row-reverse bg-gray-200 p-1 rounded">
      {[1, 2, 3].map(item => (
        <Box
          key={`flex-row-reverse-${item}`}
          className="text-sm bold flex items-center justify-center bg-gray-500 rounded-sm text-gray-100 h-8 w-8 m-1"
        >
          {item}
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox pattern="flex-col">
    <Box className="flex flex-col bg-gray-200 p-1 rounded">
      {[1, 2, 3].map(item => (
        <Box
          key={`flex-col-${item}`}
          className="text-sm bold flex items-center justify-center bg-gray-500 rounded-sm text-gray-100 h-8 w-auto m-1"
        >
          {item}
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox pattern="flex-col-reverse">
    <Box className="flex flex-col-reverse bg-gray-200 p-1 rounded">
      {[1, 2, 3].map(item => (
        <Box
          key={`flex-col-reverse-${item}`}
          className="text-sm bold flex items-center justify-center bg-gray-500 rounded-sm text-gray-100 h-8 w-auto m-1"
        >
          {item}
        </Box>
      ))}
    </Box>
  </InfoBox>
</Grid>
