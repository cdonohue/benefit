---
name: Border Color
property: border-color
parent: Border
route: /border/border-color
previousRoute: /background/background-size
previousLabel: Background Size
nextRoute: /border/border-radius
nextLabel: Border Radius
---

import { Playground } from "docz"
import ConfigPropertyList from "../../components/ConfigPropertyList"
import Grid from "../../components/Grid"
import InfoBox from "../../components/InfoBox"
import { Box, ConfigConsumer } from "../../../dist/react.js"

import UtilityInfo from "../../components/UtilityInfo"

<UtilityInfo pattern="border-{color}" themeProperty="borderColor" hasColors />

---

_Shown here using the `blue` palette_

<Grid>
  {[...Array(9).keys()].reverse().map((level) => (
    <InfoBox pattern={`border-blue-${(level + 1) * 100}`}>
      <Box
        key={level}
        className={`border rounded-sm h-16 border-blue-${(level + 1) * 100}`}
      />
    </InfoBox>
  ))}
</Grid>
