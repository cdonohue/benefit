---
name: Align Content
property: align-content
parent: Flexbox
---

import { Box } from "../../../dist/benefit.js"
import InfoBox from "../../components/InfoBox"
import Grid from "../../components/Grid"

<Grid minWidth="250px">
  <InfoBox caption="Start" pattern="content-start" color="teal">
    <Box className="flex flex-wrap h-32 content-start bg-teal-200 p-1 rounded">
      {[1, 2, 3, 4, 5].map((item) => (
        <Box key={`flex-content-start-${item}`} className="w-1/3 p-1">
          <Box className="bg-teal-500 rounded-sm text-teal-100 text-sm bold flex items-center justify-center p-2">
            {item}
          </Box>
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox caption="Center" pattern="content-center" color="teal">
    <Box className="flex flex-wrap h-32 content-center bg-teal-200 p-1 rounded">
      {[1, 2, 3, 4, 5].map((item) => (
        <Box key={`flex-content-center-${item}`} className="w-1/3 p-1">
          <Box className="bg-teal-500 rounded-sm text-teal-100 text-sm bold flex items-center justify-center p-2">
            {item}
          </Box>
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox caption="End" pattern="content-end" color="teal">
    <Box className="flex flex-wrap h-32 content-end bg-teal-200 p-1 rounded">
      {[1, 2, 3, 4, 5].map((item) => (
        <Box key={`flex-content-end-${item}`} className="w-1/3 p-1">
          <Box className="bg-teal-500 rounded-sm text-teal-100 text-sm bold flex items-center justify-center p-2">
            {item}
          </Box>
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox caption="Space Around" pattern="content-around" color="teal">
    <Box className="flex flex-wrap h-32 content-around bg-teal-200 p-1 rounded">
      {[1, 2, 3, 4, 5].map((item) => (
        <Box key={`flex-content-around-${item}`} className="w-1/3 p-1">
          <Box className="bg-teal-500 rounded-sm text-teal-100 text-sm bold flex items-center justify-center p-2">
            {item}
          </Box>
        </Box>
      ))}
    </Box>
  </InfoBox>
  <InfoBox caption="Space Between" pattern="content-between" color="teal">
    <Box className="flex flex-wrap h-32 content-between bg-teal-200 p-1 rounded">
      {[1, 2, 3, 4, 5].map((item) => (
        <Box key={`flex-content-between-${item}`} className="w-1/3 p-1">
          <Box className="bg-teal-500 rounded-sm text-teal-100 text-sm bold flex items-center justify-center p-2">
            {item}
          </Box>
        </Box>
      ))}
    </Box>
  </InfoBox>
</Grid>
