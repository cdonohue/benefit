---
name: Letter Spacing
property: letter-spacing
parent: Typography
route: /typography/letter-spacing
previousRoute: /typography/font-weight
previousLabel: Font Weight
nextRoute: /typography/line-height
nextLabel: Line Height
---

import { Playground } from "docz"
import { Box, ConfigConsumer } from "../../../dist/benefit.js"
import InfoBox from "../../components/InfoBox"
import Grid from "../../components/Grid"
import ConfigPropertyList from "../../components/ConfigPropertyList"

<ConfigConsumer>
  {({ config }) => (
    <Grid minWidth="150px">
      {Object.keys(config.theme.letterSpacing).map((size) => (
        <InfoBox key={`tracking-${size}`} pattern={`tracking-${size}`}>
          <Box className={`bg-white font-sans tracking-${size}`}>
            Pack my box with five dozen liquor jugs.
          </Box>
        </InfoBox>
      ))}
    </Grid>
  )}
</ConfigConsumer>

<ConfigPropertyList property="letterSpacing" />
