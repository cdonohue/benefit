---
name: Typography
route: /typography
order: 1600
---

import { Playground } from "docz"
import {
  Box,
  Container,
  ConfigProvider,
  ConfigConsumer,
  Heading,
  Text,
  Paragraph,
  List,
  ListItem,
} from "../src"
import SectionHeading from "./components/SectionHeading"
import ConfigPropertyList from "./components/ConfigPropertyList"
import FontBox from "./components/FontBox"
import LetterBox from "./components/LetterBox"
import TypeUtilityBox from "./components/TypeUtilityBox"

# Typography

<SectionHeading pattern="font-{family}">Font Family</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box className="bg-cool-gray-50 p-4 rounded-lg">
      {Object.keys(config.theme.fontFamily).map((fontClass) => (
        <Box className="bg-white rounded shadow mb-4 overflow-hidden">
          <Box className="p-4">
            <Box className="font-mono text-sm text-cool-gray-200">
              font-{fontClass}
            </Box>
            <Box className={`font-${fontClass}`}>
              The quick brown fox jumps over the lazy dog
            </Box>
          </Box>
          <Box className="flex flex-wrap bg-cool-gray-50 p-4">
            {config.theme.fontFamily[fontClass].map((typeface) => (
              <Box className="bg-white shadow px-3 py-1 text-xs rounded-sm mr-2 mb-2">
                {typeface}
              </Box>
            ))}
          </Box>
        </Box>
      ))}
    </Box>
  )}
</ConfigConsumer>

<SectionHeading pattern="leading-{size}">Line Height</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box className="bg-cool-gray-50 p-4 rounded-lg">
      {Object.keys(config.theme.lineHeight).map((size) => (
        <Box className="bg-white p-4 mb-4 rounded shadow">
          <Box className="font-mono text-sm text-cool-gray-200">
            leading-{size}
          </Box>
          <Box className={`leading-${size}`}>
            Portland flannel trust fund plaid kitsch. Hot chicken everyday carry
            williamsburg YOLO green juice lomo. Bicycle rights man bun +1 shabby
            chic migas salvia listicle polaroid paleo.
          </Box>
        </Box>
      ))}
    </Box>
  )}
</ConfigConsumer>

<SectionHeading pattern="tracking-{size}">Letter Spacing</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box className="bg-cool-gray-50 p-4 rounded-lg">
      {Object.keys(config.theme.letterSpacing).map((size) => (
        <Box className="bg-white p-4 mb-4 rounded shadow">
          <Box className="font-mono text-sm text-cool-gray-200">
            tracking-{size}
          </Box>
          <Box className={`tracking-${size}`}>
            The face of the moon was in shadow.
          </Box>
        </Box>
      ))}
    </Box>
  )}
</ConfigConsumer>

<SectionHeading pattern="text-{size}">Font Sizes</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box
      className="bg-cool-gray-50 rounded-lg p-4"
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(auto-fill, minmax(6rem, 1fr))",
        gridGap: "1rem",
      }}
    >
      {Object.keys(config.theme.fontSize).map((size) => (
        <Box className="rounded overflow-hidden shadow">
          <Box
            className={`bg-white h-24 flex justify-center items-center text-${size}`}
          >
            Aa
          </Box>
          <Box className="font-mono text-sm text-center p-2 text-cool-gray-300">
            text-{size}
          </Box>
        </Box>
      ))}
    </Box>
  )}
</ConfigConsumer>
